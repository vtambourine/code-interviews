# Sandbox: People list on React

Simple list on React.

## Installation

```
npm install
npm start
```

Now application in accesible at `http://localhost:3000`.

## Browser support

Tested in Chrome, Opera, Firefox, IE8+.

